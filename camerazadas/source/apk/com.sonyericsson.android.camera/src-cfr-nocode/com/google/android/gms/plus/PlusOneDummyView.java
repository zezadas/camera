/*
 * Decompiled with CFR 0_100.
 */
package com.google.android.gms.plus;

import android.content.Context;
import android.graphics.Point;
import android.graphics.drawable.Drawable;
import android.widget.FrameLayout;

public class PlusOneDummyView
extends FrameLayout {
    public static final String TAG = "PlusOneDummyView";

    public PlusOneDummyView(Context var1, int var2);

    private Point eQ(int var1);

    private d na();

    /*
     * Exception performing whole class analysis.
     * Exception performing whole class analysis ignored.
     */
    private static class a
    implements d {
        private Context mContext;

        private a(Context var1);

        /* synthetic */ a(Context var1,  var2);

        @Override
        public Drawable getDrawable(int var1);

        @Override
        public boolean isValid();
    }

    /*
     * Exception performing whole class analysis.
     * Exception performing whole class analysis ignored.
     */
    private static class b
    implements d {
        private Context mContext;

        private b(Context var1);

        /* synthetic */ b(Context var1,  var2);

        @Override
        public Drawable getDrawable(int var1);

        @Override
        public boolean isValid();
    }

    /*
     * Exception performing whole class analysis.
     * Exception performing whole class analysis ignored.
     */
    private static class c
    implements d {
        private Context mContext;

        private c(Context var1);

        /* synthetic */ c(Context var1,  var2);

        @Override
        public Drawable getDrawable(int var1);

        @Override
        public boolean isValid();
    }

    private static interface d {
        public Drawable getDrawable(int var1);

        public boolean isValid();
    }

}

